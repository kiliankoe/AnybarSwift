// swift-tools-version:5.1

import PackageDescription

let package = Package(
    name: "Anybar",
    products: [
        .library(
            name: "Anybar",
            targets: ["Anybar"]),
    ],
    dependencies: [],
    targets: [
        .target(
            name: "Anybar",
            dependencies: []),
    ]
)
